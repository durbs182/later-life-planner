# Example worker prompt

If this file still contains the marker `REPLACE_ME`, do not modify the repository.
Explain what needs to be customized for this worker and stop.

Marker: REPLACE_ME

Replace the placeholder task below with the real worker brief.

Suggested direction for a test-focused worker:
- Add or update regression tests for a specific planner bug.
- Keep production code read-only unless the parent orchestrator explicitly assigns it.
- Run the narrowest verification that proves the test change is valid.
