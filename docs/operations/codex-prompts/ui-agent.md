# Example worker prompt

If this file still contains the marker `REPLACE_ME`, do not modify the repository.
Explain what needs to be customized for this worker and stop.

Marker: REPLACE_ME

Replace the placeholder task below with the real worker brief.

Suggested direction for a UI-focused worker:
- Adjust one isolated flow or component set only.
- Preserve the established product language and avoid the term "retirement" in user-facing copy.
- Run the narrowest verification that proves the UI change still builds or tests cleanly.
